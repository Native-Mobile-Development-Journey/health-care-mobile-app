{
  "name": "healthcare-app-functions",
  "description": "Firebase Functions for Healthcare app custom claims + appointment lifecycle",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "deploy:scheduler": "firebase deploy --only functions:expireOldAppointments"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^11.10.1",
    "firebase-functions": "^4.5.0"
  }
}
